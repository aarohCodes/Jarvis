# Copy this file to .env and fill in your laptop's LAN IP address.
# Expo Go on your phone is a separate device on the network, so
# "localhost" won't reach the backend running on your laptop.
# Find your IP with `ipconfig` (Windows) and look for IPv4 Address.
EXPO_PUBLIC_API_URL=http://192.168.1.100:8001
